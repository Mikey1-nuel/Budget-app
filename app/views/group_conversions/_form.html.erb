<%= form_with(model: group_conversion) do |form| %>
  <% if group_conversion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_conversion.errors.count, "error") %> prohibited this group_conversion from being saved:</h2>

      <ul>
        <% group_conversion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.label :conversion_id, style: "display: block" %>
    <%= form.text_field :conversion_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
